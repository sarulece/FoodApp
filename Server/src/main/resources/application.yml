spring:
  datasource:
    url: ${MYSQL_CI_URL}
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8081
  
  
# ${MYSQL_CI_URL}
# ${MYSQL_USER}
# ${MYSQL_PASSWORD}
#35495e1177627b31df987b4002f9f492